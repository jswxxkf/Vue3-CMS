# https://editorconfig.org

root = true;

[*] # 表示所有文件适用
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf # 控制换行类型(lf, crlf, cr)
trim_trailing_whitespace = true
insert_final_newline = true

[*.md]  # 表示仅md文件适用以下规则
max_line_length = off
trim_trailing_whitespace = false
